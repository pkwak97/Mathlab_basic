
a = rand(3,4)

열을 없애는 법 a(:,2) = []

a array를 다 1로 만드는 방법
a[:,:] = 1

마방진을 만드는 방법
c = magic(3)

1./c 
-> c array를 역수로 만들어줌 (1로 나눠준 값)

c.^ -> 오류 발생 (demension이 define 되어있지 않아서)
c.^2 -> c의 모든 demension에 제곱을 해줌

scholor is specially treated 

demension의 크기가 같아야 연산을 해줄 수 있지만
스칼라는 예외이다.
